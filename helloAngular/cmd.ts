export class Cmd {
    // ng new project 创建项目
    // 参数generate是用来生成文件的，参数component是说明我们要生成一个组件，login呢是我们的组件名称
    // ng g c components/Members/login -it -is
    // ng generate component components/Members/login --inline-template --inline-style

    // 建立服务
    // ng g s core/auth
}